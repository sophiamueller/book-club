import reducer from './reducer'
import { handleCheck } from './actions'
// import * as Actions from './actions' gesammelt importieren

describe('reducer', () => {
  it('has a test', () => {
    expect(true).toEqual(true)
  })

  //   it('can handleCheck'() => {
  //       const state = {}
  //   })
})
